// Vital Preset: Keystation
// Auto-generated by vital2strudel

// Load wavetables
await samples({
  "_base": "/vital-wavetables/",
  "keystation_osc1": ["keystation_osc1.wav"],
  "keystation_osc2": ["keystation_osc2.wav"],
  "keystation_osc3": ["keystation_osc3.wav"],
})

// Pattern — change the notes to taste!
stack(
  note("c4 e4 g4 b4")
    .s("keystation_osc1")
    .unison(3)
    .detune(2.363)
    .spread(0.0)
    .gain(0.163)
    // envelope
    .attack(0.347)
    .release(1.379)
    // filter
    .lpf(261)
    .lpq(1.2)
    .ftype("24db")
    // effects
    .room(0.25),
  note("c3 e3 g3 b3")
    .s("keystation_osc2")
    .gain(0.112)
    // envelope
    .attack(0.347)
    .release(1.379)
    // filter
    .lpf(261)
    .lpq(0.8)
    // effects
    .room(0.25),
  note("c4 e4 g4 b4")
    .s("keystation_osc3")
    .unison(12)
    .detune(3.422)
    .gain(0.01)
    // envelope
    .attack(0.347)
    .release(1.379)
    // filter
    .lpf(261)
    .lpq(0.8)
    // effects
    .room(0.25)
)

// === Unmapped modulations (Vital-specific) ===
// random → osc_3_detune_range (amount: 0.002)
// lfo_1 → osc_3_distortion_amount (amount: 0.47)
// lfo_5 → modulation_34_amount (amount: 0.184)
// random_1 → osc_1_pan (amount: 0.078)
