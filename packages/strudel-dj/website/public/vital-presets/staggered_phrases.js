// Vital Preset: Staggered Phrases
// Auto-generated by vital2strudel

// Load wavetables
await samples({
  "_base": "/vital-wavetables/",
  "staggered_phrases_osc1": ["staggered_phrases_osc1.wav"],
  "staggered_phrases_osc3": ["staggered_phrases_osc3.wav"],
})

// Pattern — change the notes to taste!
stack(
  note("c3 e3 g3 b3")
    .s("staggered_phrases_osc1")
    .unison(14)
    .detune(1.32)
    .gain(0.452)
    // envelope
    .attack(0.329)
    .release(0.548)
    // filter
    .lpf(1589)
    .lpq(11.5)
    .ftype("ladder")
    // effects
    .coarse(9)
    .compressor("-10:20:10:.002:.02"),
  note("c3 e3 g3 b3")
    .s("staggered_phrases_osc3")
    .begin(0.336)
    .gain(0.639)
    // envelope
    .attack(0.329)
    .release(0.548)
    // effects
    .coarse(9)
    .compressor("-10:20:10:.002:.02")
)

// === Unmapped modulations (Vital-specific) ===
// lfo_1 → osc_1_wave_frame (amount: 1.0)
// lfo_2 → lfo_1_tempo (amount: 0.552)
// lfo_1 → distortion_drive (amount: -0.862)
// lfo_3 → modulation_1_amount (amount: 1.0)
