// Vital Preset: Easy Mallet
// Auto-generated by vital2strudel

// Load wavetables
await samples({
  "_base": "/vital-wavetables/",
  "easy_mallet_osc1": ["easy_mallet_osc1.wav"],
  "easy_mallet_osc2": ["easy_mallet_osc2.wav"],
})

// Pattern — change the notes to taste!
stack(
  note("c5 e5 g5 b5")
    .s("easy_mallet_osc1")
    .unison(3)
    .spread(0.0)
    .gain(0.01)
    // envelope
    .attack(0.397)
    .sustain(0.755)
    .release(0.904)
    // effects
    .room(0.45)
    .roomsize(0.91)
    .shape(0.384),
  note("c4 e4 g4 b4")
    .s("easy_mallet_osc2")
    .gain(0.01)
    // envelope
    .attack(0.397)
    .sustain(0.755)
    .release(0.904)
    // effects
    .room(0.45)
    .roomsize(0.91)
    .shape(0.384)
)

// === Unmapped modulations (Vital-specific) ===
// lfo_2 → modulation_9_amount (amount: 0.5)
// lfo_2 → modulation_13_amount (amount: 0.078)
