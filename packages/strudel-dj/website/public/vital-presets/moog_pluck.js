// Vital Preset: Moog Pluck
// Auto-generated by vital2strudel

// Load wavetables
await samples({
  "_base": "/vital-wavetables/",
  "moog_pluck_osc1": ["moog_pluck_osc1.wav"],
  "moog_pluck_osc2": ["moog_pluck_osc2.wav"],
})

// Pattern — change the notes to taste!
stack(
  note("c3 e3 g3 b3")
    .s("moog_pluck_osc1")
    .begin(0.413)
    .gain(0.683)
    // envelope
    .attack(0.369)
    .sustain(0.558)
    .release(0.571)
    // filter
    .lpf(29)
    .ftype("24db")
    .lpenv(6.2)
    .lpsustain(0.232)
    .lprelease(0.548)
    // effects
    .room(0.025),
  note("c5 e5 g5 b5")
    .s("moog_pluck_osc2")
    .begin(0.405)
    .unison(5)
    .detune(3.1)
    .gain(0.239)
    // envelope
    .attack(0.369)
    .sustain(0.558)
    .release(0.571)
    // filter
    .lpf(29)
    .ftype("24db")
    .lpenv(6.2)
    .lpsustain(0.232)
    .lprelease(0.548)
    // effects
    .room(0.025)
)

// === Unmapped modulations (Vital-specific) ===
// macro_control_2 → env_1_attack (amount: 0.14)
// macro_control_3 → filter_fx_cutoff (amount: 1.0)
