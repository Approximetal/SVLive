// Vital Preset: THUNK
// Auto-generated by vital2strudel

// Load wavetables
await samples({
  "_base": "/vital-wavetables/",
  "thunk_osc1": ["thunk_osc1.wav"],
  "thunk_osc2": ["thunk_osc2.wav"],
})

// Pattern — change the notes to taste!
stack(
  note("c4 e4 g4 b4")
    .s("thunk_osc1")
    .gain(0.547)
    // envelope
    .attack(0.149)
    .release(0.548)
    // filter
    .lpf(122)
    .lpq(1.0)
    .lpenv(4.4)
    .lpdecay(1.0)
    .lpsustain(0)
    // pitch
    .penv(16.3)
    .pdecay(1.0)
    .pcurve(1)
    // effects
    .shape(0.523),
  note("c4 e4 g4 b4")
    .s("thunk_osc2")
    .gain(0.01)
    // envelope
    .attack(0.149)
    .release(0.548)
    // effects
    .shape(0.523)
)

// === Unmapped modulations (Vital-specific) ===
// lfo_1 → compressor_mix (amount: 1.0)
// lfo_1 → modulation_4_power (amount: -0.537)
// macro_control_1 → modulation_1_amount (amount: 0.46)
// macro_control_2 → modulation_4_amount (amount: 0.146)
// lfo_1 → distortion_drive (amount: 0.16)
