// Vital Preset: VLT Future Gun
// Auto-generated by vital2strudel

// Load wavetables
await samples({
  "_base": "/vital-wavetables/",
  "vlt_future_gun_osc1": ["vlt_future_gun_osc1.wav"],
  "vlt_future_gun_osc2": ["vlt_future_gun_osc2.wav"],
  "vlt_future_gun_osc3": ["vlt_future_gun_osc3.wav"],
})

// Pattern — change the notes to taste!
stack(
  note("c4 e4 g4 b4")
    .s("vlt_future_gun_osc1")
    .gain(0.202)
    // envelope
    .attack(0.149)
    .decay(0.96)
    .release(0.402)
    // filter
    .bpf(262)
    .bpq(7.5)
    .tremolosync(2.0)
    .tremolodepth(0.82)
    // effects
    .shape(0.71),
  note("c4 e4 g4 b4")
    .s("vlt_future_gun_osc2")
    .gain(0.242)
    // envelope
    .attack(0.149)
    .decay(0.96)
    .release(0.402)
    // filter
    .bpf(262)
    .bpq(7.5)
    .tremolosync(2.0)
    // effects
    .shape(0.71),
  note("c4 e4 g4 b4")
    .s("vlt_future_gun_osc3")
    .gain(0.517)
    // envelope
    .attack(0.149)
    .decay(0.96)
    .release(0.402)
    // filter
    .lpf(259)
    .vib(2.0)
    .vibmod(24.0)
    // effects
    .shape(0.71)
)

// === Unmapped modulations (Vital-specific) ===
// lfo_1 → osc_1_spectral_morph_amount (amount: -0.672)
// lfo_1 → osc_2_spectral_morph_amount (amount: 0.324)
// macro_control_3 → modulation_9_amount (amount: 0.236)
// macro_control_3 → modulation_11_amount (amount: 0.5)
// lfo_1 → sample_level (amount: -0.199)
