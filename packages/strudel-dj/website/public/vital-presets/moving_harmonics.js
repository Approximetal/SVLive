// Vital Preset: Moving Harmonics
// Auto-generated by vital2strudel

// Load wavetables
await samples({
  "_base": "/vital-wavetables/",
  "moving_harmonics_osc1": ["moving_harmonics_osc1.wav"],
  "moving_harmonics_osc2": ["moving_harmonics_osc2.wav"],
  "moving_harmonics_osc3": ["moving_harmonics_osc3.wav"],
})

// Pattern — change the notes to taste!
stack(
  note("c4 e4 g4 b4")
    .s("moving_harmonics_osc1")
    .unison(3)
    .gain(0.593)
    // envelope
    .attack(0.149)
    .decay(0.929)
    .release(0.321),
  note("c4 e4 g4 b4")
    .s("moving_harmonics_osc2")
    .begin(0.01)
    .gain(0.839)
    // envelope
    .attack(0.149)
    .decay(0.929)
    .release(0.321)
    // filter
    .hpf(455)
    .ftype("ladder"),
  note("c5 e5 g5 b5")
    .s("moving_harmonics_osc3")
    .unison(6)
    .detune(3.2)
    .gain(0.411)
    // envelope
    .attack(0.149)
    .decay(0.929)
    .release(0.321)
    // filter
    .hpf(455)
    .ftype("ladder")
)

// === Unmapped modulations (Vital-specific) ===
// lfo_1 → osc_1_spectral_morph_amount (amount: 1.0)
// env_2 → filter_fx_cutoff (amount: 0.62)
// env_1 → modulation_9_amount (amount: 0.235)
