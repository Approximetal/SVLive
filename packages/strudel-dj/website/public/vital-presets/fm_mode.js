// Vital Preset: FM Mode
// Auto-generated by vital2strudel

// Load wavetables
await samples({
  "_base": "/vital-wavetables/",
  "fm_mode_osc1": ["fm_mode_osc1.wav"],
  "fm_mode_osc2": ["fm_mode_osc2.wav"],
})

// Pattern — change the notes to taste!
stack(
  note("c2 e2 g2 b2")
    .s("fm_mode_osc1")
    .gain(0.658)
    // envelope
    .attack(0.149)
    .release(0.999)
    // filter
    .lpf(81)
    .lpq(2.4)
    .vib(4.0)
    .vibmod(21.6)
    // effects
    .room(0.57)
    .shape(0.313)
    .compressor("-10:20:10:.002:.02"),
  note("g4 b4 d5 gb5")
    .s("fm_mode_osc2")
    .gain(0.01)
    // envelope
    .attack(0.149)
    .release(0.999)
    // filter
    .lpf(127)
    .lpq(3.4)
    .ftype("ladder")
    // effects
    .room(0.57)
    .shape(0.313)
    .compressor("-10:20:10:.002:.02")
)

// === Unmapped modulations (Vital-specific) ===
// lfo_1 → osc_2_wave_frame (amount: 0.08)
// lfo_1 → compressor_release (amount: -0.74)
// lfo_1 → reverb_dry_wet (amount: -0.375)
// lfo_1 → distortion_mix (amount: 0.58)
// lfo_1 → distortion_drive (amount: 0.05)
// lfo_1 → distortion_filter_cutoff (amount: 0.338)
// lfo_1 → filter_1_resonance (amount: 0.221)
// lfo_1 → filter_2_resonance (amount: 0.229)
// macro_control_1 → modulation_2_amount (amount: 0.178)
// lfo_2 → reverb_dry_wet (amount: -0.535)
