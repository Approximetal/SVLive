// Vital Preset: Drowning Machine
// Auto-generated by vital2strudel

// Load wavetables
await samples({
  "_base": "/vital-wavetables/",
  "drowning_machine_osc1": ["drowning_machine_osc1.wav"],
  "drowning_machine_osc2": ["drowning_machine_osc2.wav"],
  "drowning_machine_osc3": ["drowning_machine_osc3.wav"],
})

// Pattern — change the notes to taste!
stack(
  note("c4 e4 g4 b4")
    .s("drowning_machine_osc1")
    .unison(3)
    .detune(1.872)
    .gain(0.58)
    // envelope
    .decay(0.774)
    .sustain(0.9)
    .release(1.297)
    // filter
    .bpf(1243)
    .bpq(10.7)
    // effects
    .shape(0.413)
    .compressor("-10:20:10:.002:.02"),
  note("c3 e3 g3 b3")
    .s("drowning_machine_osc2")
    .begin(0.107)
    .unison(3)
    .detune(2.05)
    .gain(0.58)
    // envelope
    .decay(0.774)
    .sustain(0.9)
    .release(1.297)
    // filter
    .bpf(1243)
    .bpq(10.7)
    // effects
    .shape(0.413)
    .compressor("-10:20:10:.002:.02"),
  note("c2 e2 g2 b2")
    .s("drowning_machine_osc3")
    .gain(0.58)
    // envelope
    .decay(0.774)
    .sustain(0.9)
    .release(1.297)
    // filter
    .bpf(1243)
    .bpq(10.7)
    // effects
    .shape(0.413)
    .compressor("-10:20:10:.002:.02")
)

// === Unmapped modulations (Vital-specific) ===
// env_1 → compressor_high_gain (amount: 0.283)
// macro_control_1 → env_1_attack (amount: 0.865)
// macro_control_2 → modulation_8_amount (amount: -0.1)
// macro_control_2 → modulation_9_amount (amount: 0.135)
// macro_control_2 → modulation_10_amount (amount: 0.095)
// macro_control_1 → env_2_attack (amount: 0.367)
// macro_control_2 → reverb_delay (amount: 0.08)
