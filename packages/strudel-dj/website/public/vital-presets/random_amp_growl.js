// Vital Preset: Random Amp Growl
// Auto-generated by vital2strudel

// Load wavetables
await samples({
  "_base": "/vital-wavetables/",
  "random_amp_growl_osc1": ["random_amp_growl_osc1.wav"],
  "random_amp_growl_osc2": ["random_amp_growl_osc2.wav"],
  "random_amp_growl_osc3": ["random_amp_growl_osc3.wav"],
})

// Pattern — change the notes to taste!
stack(
  note("c2 e2 g2 b2")
    .s("random_amp_growl_osc1")
    .begin(0.332)
    .gain(0.01)
    // envelope
    .attack(0.654)
    .release(0.58)
    // filter
    .lpf(157)
    .lpq(13.2)
    // effects
    .shape(0.443)
    .compressor("-10:20:10:.002:.02"),
  note("c2 e2 g2 b2")
    .s("random_amp_growl_osc2")
    .begin(0.117)
    .gain(0.01)
    // envelope
    .attack(0.654)
    .release(0.58)
    // filter
    .lpf(157)
    .lpq(13.2)
    // effects
    .shape(0.443)
    .compressor("-10:20:10:.002:.02"),
  note("c2 e2 g2 b2")
    .s("random_amp_growl_osc3")
    .gain(0.01)
    // envelope
    .attack(0.654)
    .release(0.58)
    // effects
    .shape(0.443)
    .compressor("-10:20:10:.002:.02")
)

// === Unmapped modulations (Vital-specific) ===
// macro_control_1 → filter_fx_cutoff (amount: 0.104)
// macro_control_1 → eq_low_cutoff (amount: 0.521)
// macro_control_1 → eq_low_resonance (amount: 0.334)
