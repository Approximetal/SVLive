// Vital Preset: Memory Leak
// Auto-generated by vital2strudel

// Load wavetables
await samples({
  "_base": "/vital-wavetables/",
  "memory_leak_osc1": ["memory_leak_osc1.wav"],
  "memory_leak_osc2": ["memory_leak_osc2.wav"],
  "memory_leak_osc3": ["memory_leak_osc3.wav"],
})

// Pattern — change the notes to taste!
stack(
  note("c4 e4 g4 b4")
    .s("memory_leak_osc1")
    .begin(0.48)
    .unison(2)
    .detune(2.522)
    .gain(0.547)
    // envelope
    .attack(0.601)
    .release(0.714)
    .vib(4.0)
    .vibmod(24.0)
    // effects
    .delay(1.0)
    .delaytime(0.111)
    .delayfeedback(0.03)
    .coarse(11)
    .compressor("-10:20:10:.002:.02"),
  note("c3 e3 g3 b3")
    .s("memory_leak_osc2")
    .gain(0.01)
    // envelope
    .attack(0.601)
    .release(0.714)
    // effects
    .delay(1.0)
    .delaytime(0.111)
    .delayfeedback(0.03)
    .coarse(11)
    .compressor("-10:20:10:.002:.02"),
  note("c3 e3 g3 b3")
    .s("memory_leak_osc3")
    .begin(0.473)
    .gain(0.01)
    // envelope
    .attack(0.601)
    .release(0.714)
    // effects
    .delay(1.0)
    .delaytime(0.111)
    .delayfeedback(0.03)
    .coarse(11)
    .compressor("-10:20:10:.002:.02")
)

// === Unmapped modulations (Vital-specific) ===
// lfo_3 → osc_1_spectral_morph_amount (amount: -0.387)
// random_1 → osc_1_wave_frame (amount: -0.35)
// lfo_3 → osc_1_distortion_amount (amount: -0.32)
// macro_control_2 → modulation_3_amount (amount: 0.18)
// macro_control_3 → delay_aux_frequency (amount: -0.07)
// macro_control_3 → delay_frequency (amount: -0.13)
// note_in_octave → osc_2_pan (amount: 0.255)
// note_in_octave → osc_3_pan (amount: -0.25)
