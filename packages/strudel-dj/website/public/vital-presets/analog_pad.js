// Vital Preset: Analog Pad
// Auto-generated by vital2strudel

// Load wavetables
await samples({
  "_base": "/vital-wavetables/",
  "analog_pad_osc1": ["analog_pad_osc1.wav"],
  "analog_pad_osc2": ["analog_pad_osc2.wav"],
})

// Pattern — change the notes to taste!
stack(
  note("c4 e4 g4 b4")
    .s("analog_pad_osc1")
    .unison(7)
    .detune(3.022)
    .gain(0.277)
    // envelope
    .attack(0.518)
    .sustain(0.611)
    .release(0.892)
    // filter
    .lpf(328)
    // effects
    .shape(0.163)
    .phaser(3.0),
  note("c4 e4 g4 b4")
    .s("analog_pad_osc2")
    .gain(0.095)
    // envelope
    .attack(0.518)
    .sustain(0.611)
    .release(0.892)
    // effects
    .shape(0.163)
    .phaser(3.0)
)

// === Unmapped modulations (Vital-specific) ===
// lfo_1 → osc_1_wave_frame (amount: 1.0)
// random_1 → osc_2_distortion_amount (amount: 0.155)
